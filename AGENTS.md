# STS2 Auto Play 작업 지침

이 문서는 이 저장소에서 작업하는 사람과 AI 에이전트가 따라야 할 프로젝트 기준이다.
초기 프로젝트 기획서의 방향을 유지하되, 실제 기술 검증과 현재 구현 상태를 우선한다.
사용자의 최신 요청이 이 문서와 충돌하면 사용자 요청을 우선하고, 변경된 결정은 문서에 반영한다.

## 1. 사용자와 협업하는 방식

- 사용자는 Java 기반 웹 개발에는 익숙하지만 Python, C#, 게임 모딩, 강화학습 경험은 적다.
- 새로운 문법이나 기술을 도입할 때 Java 개념과 비교해 설명한다.
- 한 번에 지나치게 많은 새 기술을 추가하지 않고, 실행 가능한 작은 단계로 진행한다.
- 코드 변경 후에는 무엇이 어떤 순서로 실행되는지 사용자가 추적할 수 있게 설명한다.
- 클래스, 메서드, 함수에는 역할을 설명하는 간결한 한국어 docstring을 유지한다.
- 중요한 자동화 작업은 실제 게임에 적용하기 전에 추천 전용 또는 단일 행동 모드로 먼저 검증한다.
- 사용자에게 필요한 수동 조작이 있으면 목적과 정확한 순서를 간결하게 요청한다.
- 확인 가능한 정보와 추정한 내용을 구분하며, 실제 게임 버전과 API 응답으로 검증한다.

## 2. 프로젝트 목표

Slay the Spire 2의 게임 상태를 구조화해 읽고 행동을 실행할 수 있는 자동 플레이 환경을 만든다.
동일한 게임 버전, 캐릭터, 난이도와 평가 조건에서 다음 접근법을 단계적으로 비교한다.

1. Rule AI
2. Search + Heuristic AI
3. Learned Value Model
4. Reinforcement Learning Agent

최종 목표는 특정 알고리즘이 반드시 최고 승률을 얻는 것이 아니다. 승률, 도달 층, HP 손실,
의사결정 시간, 오류율과 전략 패턴을 동일 조건에서 측정하고 성능 차이의 원인을 설명하는 것이다.

## 3. 현재 확정된 범위

현재 MVP 범위는 다음과 같다.

- 게임 버전: STS2 `v0.107.1`
- 환경: Windows 11, Steam 단일 플레이
- 캐릭터: 아이언클래드 우선
- 난이도: 승천 0 우선
- 대상: 전투 자동화 우선
- 입력 정보: 일반 사용자가 현재 화면과 공개 규칙으로 확인할 수 있는 정보
- 출력 행동: 카드 사용, 대상 지정, 턴 종료
- 로그: 숨겨진 원본 순서를 제거한 요약 JSONL

현재 단계에서 사용하지 않는 정보는 다음과 같다.

- 실제 뽑기 더미 순서
- 미래 RNG 결과
- 공개되지 않은 적의 다음 행동
- 미래 카드 보상이나 이벤트 결과
- 게임 내부 객체의 비공개 필드

브리지가 해당 값을 반환하더라도 `Fair Observation Filter`에서 제거하거나 순서 없는 통계로 변환한다.

## 4. 초기 기획서에서 변경된 기술 방향

초기 기획서는 C#/.NET 기반 Headless Runner와 State/Action Bridge를 직접 만들 가능성을 중심으로 작성됐다.
기술 검증 결과 현재는 다음 방향을 사용한다.

- 직접 Bridge를 처음부터 구현하지 않고 공개된 `STS2MCP` 브리지 모드를 재사용한다.
- 호환 버전은 `STS2MCP v0.4.1-v0.107.1-fix`이다.
- 브리지는 `http://127.0.0.1:15526/api/v1/singleplayer`에서 JSON 상태와 행동 API를 제공한다.
- 현재 저장소의 애플리케이션 코드는 Python 3.12와 표준 라이브러리로 구현한다.
- C#/.NET SDK는 현재 필수 개발 환경이 아니다.
- 화면 없는 headless 실행과 병렬 시뮬레이션은 아직 구현하지 않았다.
- 실제 게임 창과 커뮤니티 브리지를 사용하는 통합 환경을 먼저 안정화한다.

이 선택은 게임 연동보다 AI 비교에 집중하고, Java 개발자에게 한 번에 필요한 새 기술을 줄이기 위한 것이다.
다음 조건이 생기면 자체 C# Bridge 또는 headless host 도입을 다시 검토한다.

- 기존 브리지가 필요한 상태나 행동을 제공하지 못하는 경우
- 게임 업데이트 대응이 장기간 중단된 경우
- 렌더링 없는 대량 시뮬레이션이 Rule/Search 평가의 병목이 된 경우
- 결정론적 `reset(seed)`와 고속 `step(action)`이 반드시 필요한 단계에 도달한 경우

## 5. 현재 구현 상태

### 완료

- STS2MCP 설치와 게임 상태 조회
- 카드 사용, 대상 지정, 턴 종료 행동 검증
- 공개 정보만 남기는 Fair Observation 변환
- 상태 변화 및 턴 전환 안정화 검사
- 추천 전용 CLI
- 단일 행동 실행 CLI
- 전투 하나를 종료까지 반복 실행하는 안전한 자동 전투 루프
- 최대 행동 수, 동일 상태 반복, stale-state 검사
- JSONL 전투 로그
- 아이언클래드 시작 카드 중심의 기본 Rule AI
- 단위 테스트와 실제 첫 전투 자동 완주

### 진행 중

- Rule AI를 성능 비교 기준선으로 개선
- 즉시 처치 가능성 계산
- 필요한 방어량과 남은 에너지 계획
- 다수 적의 위협도 및 대상 선택
- 특수 카드 효과와 상태 이상 처리
- 전투 결과 지표 수집

### 아직 하지 않음

- 카드 보상, 유물, 맵, 상점, 휴식처, 이벤트 자동 선택
- 전체 Act 또는 Run 자동화
- 고정 Seed 기반 반복 평가
- Search + Heuristic AI
- Parquet/DuckDB 데이터 파이프라인
- Learned Value Model
- Gymnasium 및 강화학습
- headless 및 병렬 실행

## 6. 코드 구조

```text
src/sts2_auto_play/
├── cli.py            명령행 시작점과 실행 모드 선택
├── client.py         STS2MCP 상태 조회 및 행동 전송
├── observation.py    공개 정보 필터, 상태 fingerprint와 요약
├── rule_agent.py     Rule AI 판단과 브리지 행동 payload 변환
└── combat_runner.py  상태 안정화, 행동 반복, 종료와 로그 제어

tests/
├── test_observation.py
├── test_rule_agent.py
└── test_combat_runner.py
```

자동 전투의 호출 흐름은 다음과 같다.

```text
cli.py
→ combat_runner.py
→ client.py에서 상태 조회
→ observation.py에서 공개 정보로 변환
→ rule_agent.py에서 행동 하나 결정
→ client.py에서 행동 실행
→ combat_runner.py에서 상태 변화 확인과 로그 기록
→ 전투 종료까지 반복
```

## 7. 실행 명령

저장소 루트에서 다음 환경을 사용한다.

```powershell
$env:PYTHONPATH = "src"
```

추천만 확인한다.

```powershell
python -m sts2_auto_play.cli
```

추천한 행동 하나만 실제 게임에 적용한다.

```powershell
python -m sts2_auto_play.cli --execute
```

현재 전투 하나를 종료까지 자동 실행한다.

```powershell
python -m sts2_auto_play.cli --auto-combat
```

테스트를 실행한다.

```powershell
python -m unittest discover -s tests -v
```

## 8. Python 코드 작성 표준

- Python 3.11 이상에서 동작해야 하며 현재 기준 환경은 Python 3.12이다.
- 공개 함수와 내부 핵심 함수에 타입 힌트를 작성한다.
- 클래스, 함수, 메서드에는 한 줄 이상의 한국어 docstring을 작성한다.
- 한 함수는 하나의 명확한 책임을 갖도록 유지한다.
- 게임 통신, Observation 변환, 정책 판단, 실행 반복을 서로 분리한다.
- Rule AI는 브리지 원본 객체가 아니라 Fair Observation만 입력으로 받는다.
- 카드 이름보다 안정적인 영문 카드 ID를 우선 사용한다.
- 지역화된 한글 설명 문자열 파싱에 핵심 규칙을 의존하지 않는다.
- 외부 패키지는 필요성이 명확할 때만 추가하고, 추가 이유를 설명한다.
- 예외를 조용히 무시하지 말고 사용자에게 이해 가능한 오류로 변환한다.
- 행동 결정에는 선택 이유를 남겨 로그에서 판단을 설명할 수 있게 한다.
- 기존 사용자 변경과 무관한 파일을 수정하지 않는다.

## 9. 게임 자동화 안전 원칙

- 기본 실행은 게임 상태를 바꾸지 않는 추천 전용이어야 한다.
- 실제 행동은 `--execute` 또는 `--auto-combat`처럼 명시적인 옵션에서만 실행한다.
- 카드 행동을 보내기 직전에 상태 fingerprint를 다시 확인한다.
- 카드 한 장을 낸 뒤 손패 인덱스가 바뀌므로 상태를 다시 읽은 후 다음 행동을 결정한다.
- 턴 전환 중의 손패 0장, 에너지 0 같은 중간 상태에서 행동하지 않는다.
- 안정된 상태가 연속 확인된 이후에만 판단한다.
- 최대 행동 수와 전환 제한 시간을 둔다.
- 동일한 의사결정 상태가 반복되면 무한 루프로 판단해 중단한다.
- 알 수 없는 화면, 지원하지 않는 선택, 브리지 오류가 발생하면 추측해 행동하지 않는다.
- 실패 시 추가 행동 없이 중단하고 원인과 로그 위치를 보고한다.

## 10. 테스트와 검증 표준

- 기능 변경에는 정상 경로와 안전 중단 경로 테스트를 함께 추가한다.
- 실제 게임 없이 실행 가능한 단위 테스트를 우선 작성한다.
- 게임 API 통합 변경은 추천 모드, 단일 행동, 제한된 자동 전투 순서로 검증한다.
- 커밋 전에 전체 테스트를 실행한다.
- Python 파일 변경 후 문법 컴파일 또는 이에 준하는 검사를 수행한다.
- 실제 전투 검증 시 게임 버전, 캐릭터, 난이도와 시작 상태를 기록한다.
- 가장 좋은 결과 하나가 아니라 평균, 분산, 오류율을 보고할 수 있도록 지표를 설계한다.

현재 기본 테스트 명령은 다음과 같다.

```powershell
$env:PYTHONPATH = "src"
python -m unittest discover -s tests -v
python -m compileall -q src tests
```

## 11. Git과 GitHub 작업 표준

### 브랜치

- `main`은 실행 가능하고 검증된 코드만 보관한다.
- `main`에서 직접 개발하거나 직접 커밋하지 않는다.
- 모든 변경은 기능 브랜치에서 작업하고 PR로 병합한다.
- 원격 브랜치는 로컬 브랜치를 처음 push할 때 생성한다.
- 브랜치 이름에 `codex/` 접두사를 사용하지 않는다.

브랜치 형식은 다음과 같다.

```text
feat/기능명
fix/수정내용
refactor/대상
test/대상
docs/문서명
chore/작업명
```

예:

```text
feat/rule-ai-planner
feat/multi-enemy-combat
fix/turn-transition-sync
docs/project-guidelines
```

### 커밋

- Conventional Commits 형식을 따른다.
- 타입은 `feat`, `fix`, `refactor`, `test`, `docs`, `chore` 등 영문 표준 타입을 사용한다.
- 타입 이외의 제목과 본문은 한글로 작성한다.
- 제목은 변경 결과를 간결하게 표현한다.
- 서로 다른 목적의 변경을 한 커밋에 섞지 않는다.
- 커밋 본문에는 무엇을 왜 변경했는지 1~3줄로 설명한다.

예:

```text
feat: Rule AI 턴 계획 기능 추가

즉시 처치 가능성과 필요한 방어량을 계산해 한 턴의 행동 우선순위를 개선한다.
```

### Push와 PR

- push 전 변경 파일, 제외 파일, 테스트 결과를 확인한다.
- push할 때 사용자에게 변경 내용을 간결한 한글 description으로 보고한다.
- PR 제목도 Conventional Commits 형식을 사용한다.
- PR 본문에는 `변경 내용`, `검증`, 필요하면 `제한 사항`을 포함한다.
- 리뷰되지 않은 코드를 `main`에 직접 push하지 않는다.
- 명시적인 요청 없이 force push, rebase로 공개 이력 변경, 원격 브랜치 삭제를 하지 않는다.

## 12. 저장소와 데이터 관리

Git에 포함한다.

- `src/`
- `tests/`
- `configs/`의 재현 가능한 설정
- `reports/`의 소형 결과와 그래프
- README와 설계·결정 문서
- 대표적인 소형 테스트 fixture

Git에 포함하지 않는다.

- 게임 원본 DLL, 실행 파일과 자산
- 커뮤니티 브리지 바이너리와 복제한 외부 저장소
- `vendor/`
- `tmp/`
- `.venv/`
- `logs/`의 일반 실행 로그
- 대용량 `data/`
- 일반 모델 체크포인트
- 인증정보, 토큰, 개인 설정

초기 데이터는 로컬 SSD에 저장한다. 대량 실험 단계에서는 Run 요약을 Parquet으로 저장하고
DuckDB로 분석한다. 모든 Run의 상세 step을 영구 저장하지 않고, 오류·특이 사례·대표 샘플만 보존한다.

## 13. 저작권과 외부 의존성

- 게임을 합법적으로 소유한 로컬 환경에서만 검증한다.
- Mega Crit 또는 STS2와 공식 제휴한 프로젝트라고 표현하지 않는다.
- 게임 원본 DLL과 자산을 저장소나 배포물에 포함하지 않는다.
- 외부 프로젝트를 사용할 때 저장소, 버전, 라이선스와 변경 여부를 기록한다.
- 현재 STS2MCP는 MIT 라이선스 커뮤니티 프로젝트이며 이 저장소에는 바이너리를 포함하지 않는다.
- 게임 업데이트로 호환성이 깨질 수 있으므로 게임 버전과 브리지 버전을 함께 기록한다.

## 14. 수정된 프로젝트 로드맵

### M0 - 기술 검증: 완료

- STS2 v0.107.1과 호환 브리지 선정
- 게임 상태 조회
- 카드 사용, 대상 지정, 턴 종료
- 실제 전투 상태 변화 확인

### M1 - 최소 전투 환경: 대부분 완료

- Fair Observation
- 행동 payload
- 상태 안정화 및 stale-state 방지
- 전투 하나 자동 완주
- JSONL 로그

남은 작업:

- 고정 Seed 시작 방법 검증
- 결정론적 재현 테스트
- 지원하지 않는 전투 화면의 명시적 분류

### M2 - Rule AI 기준선: 현재 단계

우선순위:

1. 아이언클래드 시작 카드의 구조화된 효과 정의
2. 즉시 처치 가능한 공격 조합 우선
3. 필요한 방어량 계산 후 남은 에너지로 공격
4. 취약 등 공개 상태 효과의 피해 계산
5. 다수 적의 위협도와 대상 선택
6. 카드 선택 화면, 포션, 기본 특수 상황 처리
7. 여러 전투의 HP 손실, 턴 수, 행동 수, 오류율 측정

완료 기준:

- 지원 대상으로 정의한 전투에서 항상 합법 행동을 선택한다.
- 지원하지 않는 상황은 추측하지 않고 안전하게 중단한다.
- 최소 수십 회의 초기 전투에서 승률, HP 손실과 오류율을 수집한다.
- 기준선 버전과 평가 설정을 재현 가능하게 기록한다.

### M3 - Search + Heuristic AI

- Rule AI와 별도 Agent로 구현한다.
- 한 턴의 행동 트리를 깊이 제한 탐색한다.
- 생존, 즉시 처치, 피해, 방어, 에너지 효율을 평가한다.
- 동일 전투와 동일 시간 또는 탐색 예산에서 Rule AI와 비교한다.

### M4 - 전체 Run 정책과 데이터 파이프라인

- 카드 보상, 유물, 맵, 상점, 휴식처, 이벤트 정책을 별도 모듈로 추가한다.
- 전투 정책과 Run 정책을 분리한다.
- Run 요약은 Parquet, 분석은 DuckDB를 우선한다.
- headless, 병렬 실행은 실제 측정에서 병목이 확인된 후 도입한다.

### M5 - Learned Value Model

- 상태와 최종 결과 또는 향후 보상을 연결한 데이터를 구성한다.
- 검증 데이터의 예측 성능과 calibration을 확인한다.
- Search의 수동 휴리스틱과 가치 모델을 동일 조건에서 비교한다.

### M6 - Reinforcement Learning

- 안정된 Observation/Action 스키마를 Gymnasium 환경으로 감싼다.
- 가변 합법 행동을 action mask로 처리한다.
- DQN/PPO 계열을 후보로 비교한다.
- 학습 Seed와 최종 평가 Seed를 분리한다.

### M7 - 결과와 포트폴리오

- 실행 가능한 데모
- 재현 가능한 설정과 대표 로그
- 에이전트별 비교표와 그래프
- 실패 사례, 한계와 설계 결정
- 라이선스 및 설치 절차

## 15. 다음 작업

현재 브랜치의 Rule AI 기준선을 `main`에 PR로 병합한 뒤 새 기능 브랜치에서 다음 작업을 시작한다.

```text
브랜치: feat/rule-ai-planner
목표: 즉시 처치, 필요한 방어량, 남은 에너지를 고려하는 한 턴 계획기 구현
```

구현 전에 다음 테스트 시나리오를 먼저 정의한다.

- 방어보다 공격 두 장으로 즉시 처치 가능한 경우
- 처치할 수 없어 필요한 만큼 방어해야 하는 경우
- 방어 후 남은 에너지로 공격 가능한 경우
- 취약 상태에서 피해량이 달라지는 경우
- 적이 둘 이상이고 한 적만 즉시 처치 가능한 경우
- 합법 행동이 턴 종료뿐인 경우

## 16. 완료 보고 형식

작업 완료 시 다음을 간결하게 보고한다.

1. 무엇을 변경했는지
2. 어떤 테스트와 실제 검증을 수행했는지
3. 남은 제한 사항 또는 다음 작업
4. 작업 브랜치와 커밋 제목
5. push한 경우 변경 내용 description과 PR 링크
